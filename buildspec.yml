version: 0.2

phases:
  install:
    commands:
      - echo "📦 Install phase running"
  build:
    commands:
      - echo "✅ buildspec.yml was found and is running!"
artifacts:
  files:
    - '**/*'
